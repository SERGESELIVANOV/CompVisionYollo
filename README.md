## 1. Разбор библиотек, которые были рассмотрены ##

### ONNX Runtime ###

ONNX Runtime — высокопроизводительный рантайм для запуска моделей в формате ONNX на разных платформах и устройствах. Основные преимущества: промежуточный формат модели (ONNX) позволяет переносить обученные модели из PyTorch, TensorFlow и других фреймворков; поддержка аппаратных ускорителей (CUDA, TensorRT, DirectML, OpenVINO и др.); богатые API на C, C++ и Python; гибкая конфигурация сессий и оптимизаций.

Инструкцию по установки библиотеки в проект можно найти на сайтах: 
* Официальная документация и инструкции по установке: https://onnxruntime.ai
* Руководство по сборке и использованию C++ API: https://onnxruntime.ai/docs/build/

ONNX Runtime задумывался как универсальный инструмент для запуска моделей в формате ONNX, но как показала практика использования часто оставляет противоречивые впечатления. С одной стороны, он действительно предоставляет возможность работать с моделями, экспортированными из разных фреймворков, и поддерживает множество аппаратных ускорителей. Однако на практике настройка этой библиотеки оказывается довольно капризной: необходимо тщательно подбирать версии, следить за совместимостью с драйверами и дополнительными зависимостями, а подключение провайдеров вроде CUDA или TensorRT требует отдельной конфигурации и зачастую вызывает ошибки, если окружение настроено не идеально.

Даже после успешной установки производительность не всегда соответствует ожиданиям. На CPU ONNX Runtime может работать заметно медленнее, чем альтернативные решения, а на GPU ускорение не всегда включается автоматически и требует ручной настройки. Первые запуски моделей часто занимают больше времени, чем последующие, что создаёт ощущение нестабильности. В результате библиотека, которая должна была быть лёгким и быстрым решением для инференса, нередко воспринимается как тяжеловесный и требовательный инструмент, требующий значительных усилий для достижения приемлемой скорости работы.

### TensorFlow (далее TF) ###

Задача с TF в нашем проекте была заключена в том, чтобы ускорить работу программы, т.к скорость для нас была приоритетнее точности. TF по скорости сравнивали с Coffe, ONNX Runtime и OpenCV. 

Для TF нет готовой собранной библиотеки на C++, как у OpenCV, например. Данная библиотека изначально была написана на Python, который имеет не самую высокую скорость, однако мы все равно решили протестировать скорость ее выполнения. Проверяли не на Python, а с помощью API на языке C.

После сборки библиотеки и тестов на определение объекта на 1 фотографии было принято решение не использовать в дальнейшей работе TF по след. причинам:

1. Сборка библиотеки вручную на C API проблематичная
2. Скорость довольно медленная, по сравнению с OpenCV (1,6 против 1 секунды)
3. Потеряли бы много времени на исследование C API (изначально был написан неплохой код на C++ yolov5m, который стабильно хорошо работал)

### CAFFE ###
Несмотря на то, что в статьях пишут, что эта библиотека быстрая, было совершенно больше 20 попыток собрать эту библиотеку и не одна не увенчалась успехом. Библиотеку пытались собрать как с помощью CMake так и vcpkg, так же в конфигурационных файлах CMake и сборки изначально прописаны устаревшие версии Visual Studio и других программ. Так же при сборке мы заметили, что была подгружена библиотека OpenCV.  Собрать библиотеку так и не получилось.

### OpenCV совместно с моделью YOLO ###
OpenCV — это библиотека, которая предоставляет различные функции компьютерного зрения в реальном времени, захвата видео, обработки изображений и машинного обучения.

Инструкцию по установки библиотеки в проект можно найти на сайтах: 
* <https://www.geeksforgeeks.org/cpp/opencv-c-windows-setup-using-visual-studio-2019/>
* <https://friendlyuser.github.io/posts/tech/cpp/Using_OpenCV_in_C++_A_Comprehensive_Guide/>
* <https://www.opencv-srf.com/p/introduction.html>

YOLOv5 — это быстрая и простая в использовании модель. Она основана на фреймворке PyTorch.

## Вывод ##

При анализе быстрее всего обрабатывала модель yolov5 и OpenCV, она обрабатывала картинку в среднем за 1 секунду. Так же работать с моделями yolo удобно, в файлах с github имеется файл с расширением .py, для обучения моделей. Так же имеется множество моделей компьютерного зрения для разных задач, удобная и понятная документация. После тестирования yolov5, была поставлена задача найти более быструю модель из семейства yolo так как на данный момент вышла уже 11 версия данной модели. В документации сравниваются все модели и лучшей по скорости является модель 11nano, как самая современная и лёгкая. Сравнение моделей продемонстрировано на рисунке.
<img width="1438" height="871" alt="модели" src="https://github.com/user-attachments/assets/0daba595-8b82-4e71-872f-461461bd9c1c" />



## 2. Оптимизация программы детекции объектов ##
Был создан первоначальный код с использованием yolov5, средний результат обработки изображения был равен 1 секунде. После оптимизации, описанной ниже, результат стал 0,2-0,4 секунды
#### Кратко: #### 

- Введена система пресетов моделей через структуру данных, хранящую конфигурацию каждой модели
- Поддержка нескольких моделей одновременно (YOLO11 и YOLOv5) с возможностью легкого добавления новых
- Переключение между моделями осуществляется изменением одной константы
- Каждая модель может иметь свои индивидуальные пороги детекции и NMS
- Пути к файлам настраиваются в начале программы через константы
- Легкое переключение между моделями без изменения основной логики
- Возможность настройки параметров для каждого датасета индивидуально
- Простое добавление новых моделей в будущем


#### Подробно: ####

### Универсальный постпроцессинг ###

- Реализован автоматический анализ формата выхода модели
- Программа сама определяет размерности данных и порядок измерений
- Универсальная функция преобразования данных работает с любым форматом выхода
- Автоматическое определение наличия objectness на основе количества признаков
- Динамическое вычисление количества классов и смещений в данных
- Адаптация к любому количеству классов в датасете
- Работает с любым форматом выхода YOLO независимо от версии
- Автоматически адаптируется к количеству классов (80 для COCO, 1203 для LVIS)
- Поддержка моделей с objectness (YOLOv5) и без (YOLO11)
- Устойчивость к изменениям в формате экспорта ONNX

### Параметризация инференса ###

- Параметры инференса вынесены в отдельную структуру
- Параметры передаются в функции обработки, а не берутся из глобальных констант
- Каждая модель может иметь свои индивидуальные настройки порогов
- Гибкость настройки порогов для разных датасетов
- Легкая адаптация к требованиям конкретной модели

### Улучшенная обработка координат ###

- Использование современных функций ограничения значений
- Дополнительная проверка на минимальный размер рамки
- Более надежная обработка граничных случаев
- Предотвращение создания некорректных рамок
- Более стабильная работа с изображениями разных размеров

### Настраиваемые пути ###

- Все пути вынесены в константы в начале программы
- Легкое изменение путей без изменения основной логики
- Проверка существования всех необходимых файлов и папок перед началом работы
- Понятные сообщения об ошибках при отсутствии файлов

### Оптимизация функции отрисовки ###

- NMS вызывается только один раз в постпроцессинге
- Функция отрисовки просто рисует уже отфильтрованные детекции
- Безопасная обработка векторов разной длины

## 3. Используемые библиотеки ##
В нашей программе используется множество различных библиотек для разных целей. Начнём с описания тех библиотек, которые используются в основной программе.
Основной библиотекой для работы программы является библиотека «OpenCV». Эта библиотека используется для логического вывода в рамках глубокого обучения. Также используются функции рисовки на изображении, функция «NMSBoxes()», которая используется для подавления ограничивающих рамок, так как их рисуется много. Используются типы фото «MAT» из этой библиотеки, функции «imread» для загрузки изображения и «imwrite» для сохранения. Ещё с помощью этой библиотеки задаём размер фото, для модели. Используем пространство имён «cv» и «cv::dnn», из пространства имён «cv:dnn» используем функцию для загрузки модели «readNetFromONNX». 
Скачать библиотеку можно на официальном сайте: https://opencv.org/
Также используются пространства имён «std». Для сохранения результатов использовали следующие библиотеки и функции из них. Для создания папок, типа, использующегося для пути, и функция для проверки существования файла используем библиотеку «filesystem» и советующие функции «create_directories», «path» и «exists». Для генерации имён файлов используем функцию «ostringstream» из библиотеки «sstream». Для обработки выводов модели использовали функцию «clamp» для ограничения координат рамок из библиотеки «algorithm» и функцию «memcpy» из библиотеки «cstring» для копирования данных тензоров.
Так же нам понадобятся модели и встроенные скрипты из директории на github проекта ultralytics, поэтому нужно клонировать репозиторий  YOLOv5: https://github.com/ultralytics/yolov5 
и скачать модель yolov5s : 
https://github.com/ultralytics/yolov5?tab=readme-ov-file#pretrained-checkpoints 
и модель yolo11n: https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo11n.pt

## 4. Разработка графического интерфейса с помощью Qt в Visual Studio Community 2022 ##

Разработка GUI c помощью Qt Widgets в самом начале планировалась отдельно от ComputerVision.exe, т.е. консольное приложение ComputerVision.exe отдельно от графического интерфейса GUI.exe. В таком случае разработки появлялось много ошибок при запуске и компиляции.  Было принято решение взять все исходные файлы консольного приложения ComputerVision и обернуть их в граф. интерфейс на Qt, тем самым создав самостоятельное приложение без зависимости от другого .exe. Данный способ разработки оказался более удобным, работали мы уже с привычными файлами исходников, было меньше ошибок, т.к код уже был знаком и что-то быстро исправить не являлось проблемой.

Использованные инструменты и библиотеки:

1. Расширение Qt Visual Studio Tools для VSC 2022
Официальный сайт: https://doc.qt.io/qtvstools/
Добавлена через "управление расширениями" в Visual Studio

2. Библиотека Qt 6.10.1 с инструментами: QtCreator и QtInstallerFramework
Официальный сайт: https://www.qt.io/development/download

Был использован тот же код из файлов:

ComputerVision.cpp
Constans.h
Draw.h
Libraries.h
creating_folders.h
pred_and_post_processes.h

Новые файлы при разработке:

Основные исходные файлы:
main.cpp - Точка входа приложения. Создает QApplication и запускает главное окно GUI.
GUI.cpp / GUI.h - Главное окно приложения. Управляет пользовательским интерфейсом, обрабатывает события кнопок, запускает обработку изображений в отдельном потоке.
GUI.ui - Файл интерфейса Qt Designer. Определяет внешний вид главного окна (кнопки, поля ввода, прогресс-бар, логи).

Логика компьютерного зрения:
ObjectDetector.cpp / ObjectDetector.h - Класс для детекции объектов. Загружает модели YOLO, инициализирует сеть, обрабатывает изображения последовательно.
Worker.cpp / Worker.h - Класс для обработки в отдельном потоке. Выполняет тяжелые вычисления без блокировки GUI, отправляет сигналы о прогрессе.

Qt-специфичные файлы:
GUI.rc - Файл ресурсов Windows для Qt приложения.
resource.h - Заголовок с ID ресурсов.

Ход разработки:

1. Создание базового Qt приложения
Настроен новый проект GUI в Visual Studio с использованием Qt 6.10.1
Подключены необходимые модули Qt: Core, Widgets, GUI
Настроены пути к OpenCV для компиляции и линковки
Создан базовый интерфейс в Qt Designer (GUI.ui) с элементами управления

2. Интеграция логики компьютерного зрения
Перенесены заголовочные файлы из ComputerVision: Libraries.h, Constans.h, Draw.h, pred_and_post_processes.h, creating_folders.h
Создан класс ObjectDetector - обертка над логикой детекции для синхронного использования
Добавлена поддержка выбора моделей YOLO11/YOLOv5 с соответствующими весами и метками классов

3. Реализация многопоточности
Создан класс Worker для выполнения вычислений в отдельном потоке
Интегрирована система сигналов/слотов Qt для коммуникации между потоками
Добавлен прогресс-бар и логирование в реальном времени
GUI остается полностью отзывчивым во время обработки изображений

4. Оптимизация и улучшения
Добавлена обработка ошибок и валидация входных данных
Реализован выбор папок с изображениями через QFileDialog
Организовано сохранение результатов в структурированную папочную иерархию

5. Создание установщика
Настроен Qt Installer Framework для создания установщика
Добавлены иконки для приложения и установщика
Созданы ярлыки на рабочем столе и в меню Пуск
Реализован автоматический деинсталлятор через MaintenanceTool.exe

Вывод:

Создано полнофункциональное desktop-приложение для детекции объектов с современным графическим интерфейсом, которое легко устанавливается и используется на любом Windows компьютере. Основная логика компьютерного зрения успешно интегрирована из консольного приложения без потери функциональности.

## 5.Обучение модели ##
https://docs.ultralytics.com/ru/yolov5/tutorials/train_custom_data/#12-leverage-models-for-automated-labeling

Для обучение модели YOLOv5 использовалась инструкция на сайте выше, для YOLOv11 всё тоже самое за исключением исполняемой команды для запуска процесса обучения
yolo train model=yolov11n.pt data=data.yaml epochs=30 imgsz=640 batch=40
Описание характеристик указано на сайте

Для перевода файла .pt в .onnx была использована встроенная функция библиотеки YOLO:
model.export(
    format='onnx',
    imgsz=640,
    batch=1,
    half=False,
    dynamic=True,
    simplify=True,
    opset=17,
    workspace=4,
    device='cpu'
)
